{
  "info": {
    "name": "Simple Shop API (Step 1)",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Health",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/api/health",
          "host": ["{{baseUrl}}"],
          "path": ["api", "health"]
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "// Note: Pre-request scripts run before the request is sent, so the response object is not yet available.",
              "// For more information, see: https://learning.postman.com/docs/tests-and-scripts/write-scripts/pre-request-scripts/",
              "if (!pm.collectionVariables.get('baseUrl')) { pm.collectionVariables.set('baseUrl', 'http://localhost:4000'); }"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "const json = pm.response.json();",
              "pm.test('Status 200', () => pm.response.to.have.status(200));",
              "pm.test('Has success true', () => pm.expect(json.success).to.eql(true));",
              "pm.test('Has data.status', () => pm.expect(json.data).to.have.property('status'));"
            ]
          }
        }
      ]
    }
    ,
    {
      "name": "Products",
      "item": [
        {
          "name": "List products",
          "request": {
            "method": "GET",
            "url": { "raw": "{{baseUrl}}/api/products", "host": ["{{baseUrl}}"], "path": ["api", "products"] }
          },
          "event": [
            { "listen": "test", "script": { "type": "text/javascript", "exec": [
              "const j = pm.response.json();",
              "pm.test('200', () => pm.response.to.have.status(200));",
              "pm.test('data array', () => pm.expect(Array.isArray(j.data)).to.eql(true));"
            ] } }
          ]
        }
      ]
    }
  ],
  "variable": [
    { "key": "baseUrl", "value": "http://localhost:4000" }
  ]
}
