# 🚀 מדריך הפעלה מהיר - TechBasket

## לחיצות כפתור מהירות:

### 🎯 **הפעלה מהירה:**
1. **לחץ פעמיים על** `start-server.bat` - להפעלת השרת
2. **לחץ פעמיים על** `start-client.bat` - להפעלת הלקוח  
3. **לחץ פעמיים על** `tools/health-check.bat` - לבדיקת המערכת

### 🌐 **גישה לאתר:**
- אתר: http://localhost:5173
- API: http://localhost:3000/api/health

## פקודות חלופיות (Terminal):

```bash
# שרת
cd server && npm run dev

# לקוח  
cd client && npm run dev

# בדיקת מערכת
node tools/health-check-improved.js
```

## ✅ בדיקות מהירות:
- שרת פועל: http://localhost:3000/api/health
- לקוח פועל: http://localhost:5173  
- Postman: יבא `server/postman/collection.json`

## 🔧 פתרון בעיות:
אם משהו לא עובד:
1. בדוק שMongoDB פועל
2. בדוק שRedis פועל  
3. רוץ `npm install` בשתי התיקיות
4. השתמש בכלי הבדיקה ב-`tools/`